import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'main-financial-frontend',
  webDir: 'dist'
};

export default config;
